# The live platform API. This is the default so `npm run dev` shows real
# segments, real driver reports and real model risk scores.
VITE_API_URL=http://localhost:8000

# Static fixtures instead, for working on the UI with no backend running:
# VITE_API_URL=/mock-data
